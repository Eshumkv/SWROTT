<resources>

    <!-- Base application theme. -->
    <style name="rottTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="rottTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="rottTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="rottTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"></style>

    <style name="rottTheme.splashtext" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:textSize">41dp</item>
        <item name="android:layout_marginBottom">15dp</item>
        <item name="android:layout_marginTop">15dp</item>
    </style>

</resources>
